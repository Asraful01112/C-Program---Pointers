#include <stdio.h>
#include <stdlib.h>

int *arr[2][2];

int main() {
    arr[0][0] = malloc(sizeof(int));
    arr[0][1] = malloc(sizeof(int));

    *arr[0][0] = 5;
    *arr[0][1] = 10;

    printf("%d\n", *arr[0][0]);
    printf("%d\n", *arr[0][1]);

    free(arr[0][0]);
    free(arr[0][1]);
}
